Definition def: blab(asd).
Definition def: iahdslkajsdl(asd).

Lemma blub: blub(asd). 
Proof: Then blab(asd). qed.

Lemma asdasd: asdasd(asd). 
Proof: Then blab(asd). qed.


# Notation in: a ϵ A.
# Notation union: A ∪ B.
# 
# Definition: relapp(blub).
# Definition: a ϵ (A ∪ B). # in(a,union(A,B))
# Definition: A ∪ B is set. # in(a,union(A,B))