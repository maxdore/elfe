Include functions.

Let f: A -> B.
Let i: X -> X.

Definition identity: i is identity iff for all x ∈ X. (f{x}) = x.

Notation inverse: f⁻¹.

Definition inverse: (f⁻¹): B -> A and (for all x. (f{x}) = y implies ((f⁻¹){y}) = x).

Lemma: f is bijective implies (f⁻¹)∘f is identity.
Proof: qed.