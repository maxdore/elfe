Include relations.

Let R be relation.

Lemma: R ∪ (R⁻¹) is symmetric.
Proof:
    Assume (R ∪ (R⁻¹))[x,y].
    Then R[x,y] or (R⁻¹)[x,y].
    Case R[x,y]:
        Then (R⁻¹)[y,x].
    qed.
    Case (R⁻¹)[x,y]:
        Then R[y,x].
    qed.
    Hence (R ∪ (R⁻¹))[y,x].
qed.